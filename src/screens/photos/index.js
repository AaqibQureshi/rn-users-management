import React from 'react';
import {Text, View} from 'react-native';
import Photos from '../../components/Photos';
const PhotosScreen = props => {
  return <Photos navigation={props} />;
};

export default PhotosScreen;
